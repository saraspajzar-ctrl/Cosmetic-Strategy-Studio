# Frontend — copy to frontend/.env.local
# If running locally, the Vite proxy handles /api → http://localhost:8000
# For production, set this to your deployed backend URL
VITE_API_URL=http://localhost:8000
